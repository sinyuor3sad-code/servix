worker_processes auto;

events {
    worker_connections 2048;
}

http {
    # ── Access Logging (JSON structured) ──
    log_format json_combined escape=json
        '{'
            '"time":"$time_iso8601",'
            '"remote_addr":"$remote_addr",'
            '"method":"$request_method",'
            '"uri":"$request_uri",'
            '"status":$status,'
            '"body_bytes":$body_bytes_sent,'
            '"request_time":$request_time,'
            '"upstream_time":"$upstream_response_time",'
            '"http_referrer":"$http_referer",'
            '"http_user_agent":"$http_user_agent",'
            '"server_name":"$server_name",'
            '"correlation_id":"$request_id"'
        '}';

    access_log /var/log/nginx/access.log json_combined;
    error_log /var/log/nginx/error.log warn;

    # ── Docker embedded DNS — re-resolve on container restart ──
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    # ── API Load Balancing (4.1 — Horizontal Scaling) ──
    upstream api_servers {
        least_conn;
        server api-1:4000;
        server api-2:4000;
    }

    # ── WebSocket (sticky sessions via ip_hash) ──
    upstream api_ws {
        ip_hash;
        server api-1:4000;
        server api-2:4000;
    }

    # ── Rate Limiting Zones ──
    limit_req_zone $binary_remote_addr zone=api_general:10m rate=30r/s;
    limit_req_zone $binary_remote_addr zone=api_auth:10m rate=5r/m;
    limit_req_zone $binary_remote_addr zone=web_general:10m rate=20r/s;

    # ── Gzip ──
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # ── SSL Hardening ──
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # ── Security Headers (global) ──
    add_header X-Frame-Options SAMEORIGIN always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # ── Request Limits ──
    client_max_body_size 10m;
    proxy_read_timeout 60s;
    proxy_connect_timeout 10s;
    proxy_send_timeout 60s;

    # Hide nginx version
    server_tokens off;

    # HTTP → HTTPS redirect
    server {
        listen 80;
        server_name api.servi-x.com app.servi-x.com booking.servi-x.com admin.servi-x.com servi-x.com www.servi-x.com;
        return 301 https://$host$request_uri;
    }

    # www → non-www redirect
    server {
        listen 443 ssl;
        server_name www.servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        return 301 https://servi-x.com$request_uri;
    }

    # ── API (load balanced across api-1, api-2) ──
    server {
        listen 443 ssl;
        server_name api.servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        # Auth endpoints — strict rate limit
        location ~ ^/api/v1/auth/(login|register|refresh|forgot-password) {
            limit_req zone=api_auth burst=3 nodelay;
            add_header Cache-Control "no-store, no-cache" always;
            proxy_pass http://api_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Correlation-ID $request_id;
        }

        # Prometheus metrics — no cache
        location /metrics {
            proxy_pass http://api_servers;
            proxy_set_header Host $host;
            add_header Cache-Control "no-store" always;
        }

        # General API — no cache
        location / {
            limit_req zone=api_general burst=50 nodelay;
            add_header Cache-Control "no-store, no-cache" always;
            proxy_pass http://api_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Correlation-ID $request_id;
        }

        # WebSocket — sticky sessions
        location /ws {
            proxy_pass http://api_ws;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /socket.io/ {
            proxy_pass http://api_ws;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    # ── Dashboard (dynamic DNS via resolver — survives container restarts) ──
    server {
        listen 443 ssl;
        server_name app.servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        # Variable-based proxy_pass forces DNS re-resolution via resolver
        set $dashboard_upstream http://dashboard:3000;

        # Next.js hashed static assets — long cache
        location /_next/static/ {
            proxy_pass $dashboard_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        # Images
        location /images/ {
            proxy_pass $dashboard_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=604800" always;
        }

        # Fonts
        location /fonts/ {
            proxy_pass $dashboard_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        location / {
            limit_req zone=web_general burst=40 nodelay;
            proxy_pass $dashboard_upstream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ── Booking (dynamic DNS) ──
    server {
        listen 443 ssl;
        server_name booking.servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        set $booking_upstream http://booking:3001;

        location /_next/static/ {
            proxy_pass $booking_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        location / {
            limit_req zone=web_general burst=40 nodelay;
            proxy_pass $booking_upstream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ── Admin (dynamic DNS) ──
    server {
        listen 443 ssl;
        server_name admin.servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        set $admin_upstream http://admin:3002;

        location /_next/static/ {
            proxy_pass $admin_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        location / {
            limit_req zone=web_general burst=40 nodelay;
            proxy_pass $admin_upstream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ── Landing (dynamic DNS) ──
    server {
        listen 443 ssl;
        server_name servi-x.com;

        ssl_certificate /etc/nginx/ssl/origin.crt;
        ssl_certificate_key /etc/nginx/ssl/origin.key;

        set $landing_upstream http://landing:3003;

        location /_next/static/ {
            proxy_pass $landing_upstream;
            proxy_set_header Host $host;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        location / {
            limit_req zone=web_general burst=40 nodelay;
            proxy_pass $landing_upstream;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    # Stub status for Prometheus nginx-exporter (internal)
    server {
        listen 8080;
        server_name localhost;
        location /stub_status {
            stub_status;
        }
    }
}
