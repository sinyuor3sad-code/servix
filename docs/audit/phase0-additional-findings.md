# SERVIX — Phase 0: Additional Findings

> **تاريخ التنفيذ:** 2026-04-07
> **المنهجية:** اكتشافات إضافية أثناء تنفيذ المرحلة 0 — غير مذكورة في MASTER_PLAN.md

---

## اكتشافات جديدة

### 1. 🔴 ملفات الاختبار القائمة لا تُصان

| الحقيقة | التفصيل |
|---------|---------|
| المشكلة | 3 من 7 ملفات اختبار فاشلة — الكود تطور لكن الاختبارات لم تُحدّث |
| `auth.service.spec.ts` | Mock لا يحوي `plan.findFirst` — أُضيف منطق البحث عن خطة أساسية لكن الاختبار لم يتغير |
| `admin.service.spec.ts` | تبعيات NestJS مفقودة من TestingModule — أُضيفت خدمات جديدة لكن الاختبار لم يتغير |
| `tenant.middleware.spec.ts` | `PlatformSettingsService` مفقود — أُضيف كمعامل جديد لكن الاختبار لم يتغير |
| الأثر | **الاختبارات الفاشلة أسوأ من عدم وجود اختبارات** — تُعطي انطباعاً زائفاً بأن هناك تغطية |
| التوصية | إصلاح الاختبارات الـ 3 الفاشلة أولوية في المرحلة 1 قبل كتابة اختبارات جديدة |

### 2. 🟡 تصحيح في MASTER_PLAN: Product.categoryId لديه فهرس

| الحقيقة | التفصيل |
|---------|---------|
| ما ذُكر في MASTER_PLAN | `Product.categoryId — لا فهرس (tenant.prisma:887)` |
| الواقع | `@@index([categoryId])` موجود في `tenant.prisma:891` |
| التصحيح | العدد الفعلي للفهارس المفقودة = 4 وليس 5 |

### 3. 🟡 ارتباك في سكربتات الاسترجاع

| الحقيقة | التفصيل |
|---------|---------|
| المشكلة | يوجد ملفان للاسترجاع: `backup.sh --restore` و `restore.sh` |
| `backup.sh --restore` | يسترجع ملف واحد — 16 سطر فقط |
| `restore.sh` | ملف منفصل بـ 1692 بايت — غير واضح الفرق |
| الأثر | في حالة طوارئ، أي سكربت يُستخدم؟ |
| التوصية | توحيد سكربتات الاسترجاع في ملف واحد واضح |

### 4. 🟡 Console Ninja extension يتدخل في الاختبارات

| الحقيقة | التفصيل |
|---------|---------|
| المشكلة | كل ملف اختبار يُظهر `Console Ninja extension is connected to Nest.js` |
| المسار | `.cursor/extensions/wallabyjs.console-ninja-1.0.523-universal/` |
| الأثر | تلوث output الاختبارات — قد يؤثر على CI إذا كان الامتداد مُثبتاً |
| التوصية | لا إجراء مطلوب — هذا خاص ببيئة التطوير فقط |

### 5. 🟢 التغطية الحقيقية أعلى مما ذُكر

| الحقيقة | التفصيل |
|---------|---------|
| ما ذُكر في MASTER_PLAN | `تغطية < 2%` (1.4% بعدد الملفات) |
| القياس الفعلي | **9.86% statements, 9.78% lines** |
| السبب | MASTER_PLAN قاس بعدد الملفات (7/504). Jest يقيس بالأسطر المُنفذة — أكثر دقة |
| الملاحظة | الأرقام لا تزال ضعيفة جداً — لكن خط الأساس الفعلي مختلف عما ذُكر |

### 6. 🟡 بيئة التطوير المحلية ناقصة

| الحقيقة | التفصيل |
|---------|---------|
| المشكلة | بيئة التطوير على Windows لا تحوي Docker, PostgreSQL, Redis, psql, k6 |
| الأثر | لا يمكن تنفيذ 3 من 5 مهام المرحلة 0 محلياً (backup/restore, query analysis, load test) |
| التوصية | إنشاء `docker-compose.dev.yml` خفيف يعمل على Windows + توثيق في README |

### 7. 🟡 `pnpm` overrides مطلوبة في الجذر

| الحقيقة | التفصيل |
|---------|---------|
| المشكلة | `package.json` الجذر يحوي `pnpm.onlyBuiltDependencies` لكن لا يحوي `pnpm.overrides` |
| الأثر | 4 من 11 ثغرات أمنية لا يمكن إصلاحها بدون overrides (lodash, picomatch) |
| التوصية | إضافة overrides في المرحلة 1 مع الإصلاحات الأمنية |

---

## ملخص الأثر

| الفئة | العدد | الأثر |
|-------|-------|-------|
| اكتشافات تحتاج إصلاح | 3 | اختبارات فاشلة + ارتباك الاسترجاع + بيئة تطوير ناقصة |
| تصحيحات على MASTER_PLAN | 2 | فهرس Product موجود + تغطية 9.86% (وليس <2%) |
| ملاحظات معلوماتية | 2 | Console Ninja + pnpm overrides |

---

*هذا الملف جزء من تدقيق المرحلة 0 — اكتشافات إضافية.*
